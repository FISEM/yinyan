{
  "name": "yinyan",
  "version": "1.0.0",
  "private": "false",
  "description": "Handle errors as values in TypeScript. Balanced execution for sync and async flows.",
  "author": {
    "name": "Israel Emmanuel Matenly Forstal"
  },
  "keywords": [
    "typescript",
    "error-handling",
    "result-type",
    "safe-run",
    "async"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --minify --sourcemap && bun x tsc",
    "prepublishOnly": "bun run build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}